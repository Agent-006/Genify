import React from "react";

const Page = () => {
    return (
        <div className="h-full flex items-center justify-center bg-[hsl(var(--background))]">
            Domain
        </div>
    );
};

export default Page;
